From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: etil2jz <blanchot.arthur@protonmail.ch>
Date: Sat, 2 Apr 2022 23:17:59 +0200
Subject: [PATCH] Fix MC-246262


diff --git a/src/main/java/net/minecraft/world/level/levelgen/structure/templatesystem/StructureTemplate.java b/src/main/java/net/minecraft/world/level/levelgen/structure/templatesystem/StructureTemplate.java
index c5827e4d870a0bba483649d54cae23072eab2b18..0efdbaed0ebb8ba054c7c956ba0f8c00a7c51c98 100644
--- a/src/main/java/net/minecraft/world/level/levelgen/structure/templatesystem/StructureTemplate.java
+++ b/src/main/java/net/minecraft/world/level/levelgen/structure/templatesystem/StructureTemplate.java
@@ -53,6 +53,7 @@ import net.minecraft.world.phys.shapes.DiscreteVoxelShape;
 import org.bukkit.craftbukkit.persistence.CraftPersistentDataContainer;
 import org.bukkit.craftbukkit.persistence.CraftPersistentDataTypeRegistry;
 // CraftBukkit end
+import net.minecraft.world.level.WorldGenLevel; // Mirai - fix MC-246262
 
 public class StructureTemplate {
 
@@ -265,7 +266,11 @@ public class StructureTemplate {
 
                         if (definedstructure_blockinfo.nbt != null) {
                             tileentity = world.getBlockEntity(blockposition2);
-                            Clearable.tryClear(tileentity);
+                            // Mirai start - fix MC-246262
+                            if (!(world instanceof WorldGenLevel)) {
+                                Clearable.tryClear(tileentity);
+                            }
+                            // Mirai end
                             world.setBlock(blockposition2, Blocks.BARRIER.defaultBlockState(), 20);
                         }
 
@@ -380,7 +385,11 @@ public class StructureTemplate {
                         if (pair1.getSecond() != null) {
                             tileentity = world.getBlockEntity(blockposition6);
                             if (tileentity != null) {
-                                tileentity.setChanged();
+                                // Mirai start - fix MC-246262
+                                if (!(world instanceof WorldGenLevel)) {
+                                    tileentity.setChanged();
+                                }
+                                // Mirai end
                             }
                         }
                     }
